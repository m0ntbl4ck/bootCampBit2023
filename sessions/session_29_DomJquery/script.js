// continuar con JQuery

/*
$("#link").click (function(e){
e.preventDefault();// previene el comportamiento por defecto del elemento "a"
$("#texto").text("hola que tal?");// modificar el DOM un parrafo cambiar el texto

$("#texto").css("background-color", "blue");// modificar el CSS

$("#texto").faceOut (1500);// Imprementar animaciones 
// encadenamiento
$("#texto").text("hola que tal?").css("background-color", "blue").faceOut(1500);
}); 
*/
/* const colorTitle=0;

$("#cambiar").click(function () {
    const tamanoDeFuente = parseInt($("#tamanoDeFuente").val());
    const tipoDeFuente = parseInt($("#tipoDeFuente").val());
    const colorFuente = parseInt($("#colorFuente").val());

console.log(tamanoDeFuente)
console.log(tipoDeFuente)
console.log(colorFuente)
    if (tamanoDeFuente === 0) {

        $("#texto").css("font-size", "large")
    }
    if (tamanoDeFuente === 1) {
        $("#texto").css("font-size", "medium")
    }
    if (tamanoDeFuente === 2) {
        $("#texto").css("font-size", "small")
    }

    if (tipoDeFuente === 0) {
        $("#texto").css("font-family", "Arial")
    }
    if (tipoDeFuente === 1) {
        $("#texto").css("font-family", "Cursive")
    }
    if (tipoDeFuente === 2) {
        $("#texto").css("font-family", "Helvetica")
    }

    if (colorFuente === 0) {
        $("#texto").css("color", "black")
    }
    if (colorFuente === 1) {
        $("#texto").css("color", "red")
    }
    if (colorFuente === 2) {
        $("#texto").css("color", "blue")
    }

});
 */
/* 
.hide()
.show() */

$("#seccion1").click (function() {
    $("#seccionMostrar").hide;

})

//(this).data("precio")
